<UserControl x:Class="Roche_Scoreboard.Views.VideoPlayerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="420" d:DesignWidth="707"
             Background="Black">

    <!-- Uniform: preserve aspect ratio so videos are never distorted; the
         clipped black bars are visually preferable to stretched content
         and the surface remains lightweight for the renderer. -->
    <Grid ClipToBounds="True"
          RenderOptions.BitmapScalingMode="LowQuality"
          RenderOptions.EdgeMode="Aliased">
        <MediaElement x:Name="VideoPlayer"
                      LoadedBehavior="Manual"
                      UnloadedBehavior="Manual"
                      ScrubbingEnabled="True"
                      Stretch="Uniform"
                      Volume="1.0"
                      RenderOptions.BitmapScalingMode="LowQuality"
                      RenderOptions.EdgeMode="Aliased"/>
    </Grid>
</UserControl>
